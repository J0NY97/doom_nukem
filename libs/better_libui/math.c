#include "better_libui.h"

// Remove whole thing when you have this in the libft
int	min(int a, int b)
{
	if (a < b)
		return (a);
	return (b);
}

int	max(int a, int b)
{
	if (a < b)
		return (b);
	return (a);
}
